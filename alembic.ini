# alembic.ini — placeholder
# Run `alembic init alembic` to generate the full Alembic configuration.
# Then set sqlalchemy.url in alembic.ini or use env.py to read from settings.
#
# Typical usage:
#   alembic revision --autogenerate -m "initial migration"
#   alembic upgrade head
